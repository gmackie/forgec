# realization-b

Temporal + keyed services stores current entities in Postgres + object storage; Kafka carries business occurrences. Process owners preserve the ownership declared in ConceptIR. 

The sketch must satisfy the business rules in domain.md and the obligations in sketch.json. Neither the stack name nor the partial signature establishes authorization, temporal correctness, compensation, or invariant enforcement. Distinct algorithms are equivalent only if their business outcomes meet the same contract.

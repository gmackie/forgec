[package]
name = "@forgegraph/foundation/agreement-catalog"
version = "0.1.0"
edition = "2027"
[dependencies]
decision = { path = "../decision", deploy = true }
specification = { path = "../specification", deploy = true }
participation = { path = "../participation", deploy = true }
entitlement = { path = "../entitlement", deploy = true }
evaluation = { path = "../evaluation", deploy = true }
artifact = { path = "../artifact", deploy = true }
evidence = { path = "../evidence", deploy = true }
party = { path = "../party", deploy = true }

[package]
name = "@forgegraph/foundation/quotation-pricing"
version = "0.1.0"
edition = "2027"
[dependencies]
agreement-catalog = { path = "../agreement-catalog", deploy = true }
specification = { path = "../specification", deploy = true }
evaluation = { path = "../evaluation", deploy = true }
ledger = { path = "../ledger", deploy = true }
party = { path = "../party", deploy = true }

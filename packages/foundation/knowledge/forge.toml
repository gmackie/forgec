[package]
name = "@forgegraph/foundation/knowledge"
version = "0.1.0"
edition = "2027"
[dependencies]
artifact = { path = "../artifact", deploy = true }
specification = { path = "../specification", deploy = true }
classification = { path = "../classification", deploy = true }
publication = { path = "../publication", deploy = true }
entitlement = { path = "../entitlement", deploy = true }
evaluation = { path = "../evaluation", deploy = true }

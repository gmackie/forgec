[package]
name = "@forgegraph/foundation/adjudication"
version = "0.1.0"
edition = "2027"
[dependencies]
decision = { path = "../decision", deploy = true }
entitlement = { path = "../entitlement", deploy = true }
evidence = { path = "../evidence", deploy = true }
evaluation = { path = "../evaluation", deploy = true }
fulfillment = { path = "../fulfillment", deploy = true }
ledger = { path = "../ledger", deploy = true }
delivery = { path = "../delivery", deploy = true }

[package]
name = "@forgegraph/foundation/service-level"
version = "0.1.0"
edition = "2027"
[dependencies]
agreement = { path = "../agreement-catalog", deploy = true }
entitlement = { path = "../entitlement", deploy = true }
evaluation = { path = "../evaluation", deploy = true }
fulfillment = { path = "../fulfillment", deploy = true }
availability = { path = "../availability", deploy = true }
specification = { path = "../specification", deploy = true }

[package]
name = "@forgegraph/foundation/scheduling"
version = "0.1.0"
edition = "2027"
[dependencies]
availability = { path = "../availability", deploy = true }
allocation = { path = "../allocation", deploy = true }
participation = { path = "../participation", deploy = true }
place = { path = "../place", deploy = true }
fulfillment = { path = "../fulfillment", deploy = true }

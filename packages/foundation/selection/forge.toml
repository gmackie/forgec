[package]
name = "@forgegraph/foundation/selection"
version = "0.1.0"
edition = "2027"
[dependencies]
specification = { path = "../specification", deploy = true }
participation = { path = "../participation", deploy = true }
evaluation = { path = "../evaluation", deploy = true }
decision = { path = "../decision", deploy = true }
agreement-catalog = { path = "../agreement-catalog", deploy = true }
